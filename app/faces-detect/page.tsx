import useSWR from 'swr'
import HidenImages from "@/app/ui/hidenImages";

export default async function Page() {
    
    // if (isLoading) return <div>loading...</div>
    // console.log(data);
    
  return (
    <HidenImages />
  )
}
